/* globals describe, it */
'use strict'

// var expect = require('chai').expect
// var request = require('request')

describe('Authorization', function () {
  it('should fail when no authentication is provided')
  it('should accept authenticated requests with a token from facebook')
  it('should accept authenticated requests with a token from twitter')
  it('should accept authenticated requests with a token from google')
})
